# From Meta App Dashboard > WhatsApp > API Setup
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_WABA_ID=your_whatsapp_business_account_id

# Your Meta app's numeric ID (App Dashboard > Settings > Basic).
# Only needed for uploading sample media for template headers (resumable upload API).
META_APP_ID=your_meta_app_id

# Your Meta app's secret (App Dashboard > Settings > Basic, next to App ID).
# Used to verify inbound webhook POSTs actually came from Meta. Strongly
# recommended - without it, anyone could POST a fabricated payload to /webhook.
META_APP_SECRET=your_meta_app_secret

# System user permanent token (Business Settings > System users > Generate token)
WHATSAPP_ACCESS_TOKEN=your_permanent_system_user_token

# Any string you choose. Enter the same value in the Meta webhook config.
WEBHOOK_VERIFY_TOKEN=choose-a-random-string

# Login for the console itself. REQUIRED in practice - without both of these
# set, every route (conversations, broadcasts, contacts) is open to anyone
# who has the URL. Pick your own username and a strong password.
BASIC_AUTH_USER=choose-a-username
BASIC_AUTH_PASS=choose-a-strong-password

GRAPH_API_VERSION=v23.0
PORT=3000
DEFAULT_COUNTRY_CODE=91
